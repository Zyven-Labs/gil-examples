intent set_cell(X, Y)
    cell[X, Y] <= true
end

intent clear_cell(X, Y)
    cell[X, Y] <= false
end

intent shift(Dx, Dy)
    when cell[X, Y] do
        cell[X, Y] <= false
        shifted[X + Dx, Y + Dy] <= true
    end
end