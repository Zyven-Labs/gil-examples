intent set_state(State)
    active[State] <= true
end

intent transition(From, To)
    when active[From] do
        active[From] <= false
        active[To] <= true
    end
end

intent reset()
    when active[S] do
        active[S] <= false
    end
end